# Nutri Presentation

Componentes, hooks e view-models do modulo.

Padrao do projeto:

- Componentes renderizam UI.
- Hooks carregam dados, preparam estado e expoem actions.
- Regras de negocio ficam em `domain` e `application`.
