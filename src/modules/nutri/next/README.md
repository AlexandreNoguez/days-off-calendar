# Nutri Next Adapters

Adaptadores especificos de Next.js para paginas, layouts e API route handlers.

Arquivos em `app/(main)/nutri/*` e `app/api/nutri/*` devem ser finos e
reexportar esta camada. Isso facilita mover o modulo para outro projeto Next.js.
