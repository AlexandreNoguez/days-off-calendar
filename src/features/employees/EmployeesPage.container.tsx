import { EmployeesPageView } from "./EmployeesPage.view";

export function EmployeesPageContainer() {
  // Depois ligaremos com useEmployeesPage() + employeeForm.ts (react-hook-form + zod)
  return <EmployeesPageView />;
}
